# Astra Studio Release 3.17 — Test Report

Дата: 2026-09-23

- Полный Windows gate: `282 collected`, `280 passed`, `2 skipped`, `0 failed`, `88 subtests passed`.
- Два пропуска остаются платформенными: POSIX executable-bit и создание symlink, недоступное текущей учётной записи Windows.
- Реальный `g++` прогон подтвердил: случайная неверная C++-строка попадает в Problems с точным номером, а запрос на переустановку компилятора не открывается.
- Qt runtime gate подтверждает, что выбор значка и создание ярлыка принадлежат странице настроек, а не установщику.
- Статические regression-проверки подтверждают отсутствие синтетического ввода, его шаблона и старых per-panel controls; миграция старых настроек сохранена.
- Нативный визуальный smoke выполнен при `980×700`: панель настроек прокручивается, проектное дерево адаптивно скрыто, редактор и консоль не перекрыты.
- Production EXE запущен из `dist`, показал заголовок `Astra Studio — Release 3.17` и штатно завершился с кодом 0.
