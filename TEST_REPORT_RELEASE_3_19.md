# Astra Studio Release 3.19 — Test Report

Дата: 2026-09-24

## Автоматический gate

- `pytest`: **291 collected / 289 passed / 2 skipped / 0 failed**.
- Параметризованные проверки: **88 passed**.
- `python -m py_compile main.py`: PASS.
- PyInstaller onedir build: PASS.
- Production EXE: запущен из собранной папки, процесс отвечал, заголовок `Astra Studio — Release 3.19` подтверждён.

Два пропуска остаются ожидаемыми Windows permission/platform checks и не скрывают падений. Предупреждение pytest относится только к запрету OneDrive на запись `.pytest_cache` и не затрагивает приложение или тестовые результаты.

## Регрессии Release 3.19

- Реальные Qt key events подтверждают: первый Tab выбирает вариант без вставки, Space подтверждает выбранное дополнение, Space без предварительного выбора вставляется как обычный символ.
- Проверка кнопки обновления с намеренно недоступным bundled JSON подтверждает переход на встроенный публичный manifest URL.
- Макеты 1366×768 и 1600×900 подтверждают расширенную панель настроек и временно скрытое дерево проекта.
- Один и тот же wallpaper tile остаётся неизменным после изменения размера окна; итоговый canvas точно совпадает с рабочей областью.
- В production-папке присутствуют `_internal/update_channel.json` и `_internal/scripts/create_desktop_shortcut.ps1`.
- Изолированные updater-тесты подтверждают замену portable-папки, сохранение предыдущей версии и откат при раннем завершении новой сборки.

## Визуальная проверка

Снимки с настоящим Qt layout сгенерированы для 1366×768 и 1600×900. Проверено, что настройки не зажаты деревом проекта, редактор и консоль остаются читаемыми, а Angel 404 остаётся в фиксированном масштабе и попадает в видимую область.

Ручная проверка на втором физическом ноутбуке остаётся owner acceptance и не подменяется этим отчётом.
